Description: Fix deprecated string interpolation.
Author: Rinat Gumirov <rinat.gumirov66@gmail.com>
Forwarded: not-needed
Last-Update: 2025-11-28

--- phoronix-test-suite-10.8.4.orig/pts-core/commands/check_tests.php
+++ phoronix-test-suite-10.8.4/pts-core/commands/check_tests.php
@@ -244,11 +244,11 @@
 			$secs = floor($secs);
 
 		if ($hours == 0 && $mins == 0)
-			return "${secs}s";
+			return "{$secs}s";
 		else if ($hours == 0)
-			return "${mins}m ${secs}s";
+			return "{$mins}m {$secs}s";
 		else
-			return "${hours}h ${mins}m ${secs}s";
+			return "{$hours}h {$mins}m {$secs}s";
 	}
 
 	/**
